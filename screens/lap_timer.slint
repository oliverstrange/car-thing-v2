import { VerticalBox } from "std-widgets.slint";
import { MenuItem } from "../components/MenuItem.slint";

export component LapTimer {
    width: 240px;
    height: 240px;
    
    VerticalBox {
        alignment: center;

        MenuItem {
            text: "Lap Timer Screen";
            selected: false;
        }
    }
}