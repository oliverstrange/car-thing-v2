import { VerticalBox } from "std-widgets.slint";
import { MenuItem } from "../components/MenuItem.slint";

export component OilPressure {
    width: 240px;
    height: 240px;
    
    VerticalBox {
        alignment: center;

        MenuItem {
            text: "Oil Pressure Screen";
            selected: false;
        }
    }
}